name: Shell commands test

on: [push]

jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: ecdho "Hello world"
      - name: multiline script  
        run: |
          node -v
          npm -v